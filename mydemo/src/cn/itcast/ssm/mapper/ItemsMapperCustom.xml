<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.itcast.ssm.mapper.ItemsMapperCustom" >
  <sql id="query_items_where">
  <if test="itemsCustom!=null">
  <if test="itemsCustom.name!=null and itemsCustom.name!=''">
       items.name like '%${itemsCustom.name}%'
  </if>
  </if>
  </sql>
  <select id="findItemsList" parameterType="cn.itcast.ssm.po.ItemsQueryVo" resultType="cn.itcast.ssm.po.ItemsCustom">
 SELECT items.* from items
 <where>
  <include refid="query_items_where"></include>
 </where>
  </select>
  
   <!--批量删除 -->
<delete id="deleteItemsByLists" parameterType="java.lang.Integer">
          delete from items where id in
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    
    <!-- 添加商品信息 -->
    <insert id="insertItems" parameterType="cn.itcast.ssm.po.ItemsCustom" >
    insert into items ( name, price, 
      pic, createtime, detail
      )
    values ( #{name,jdbcType=VARCHAR}, #{price,jdbcType=REAL}, 
      #{pic,jdbcType=VARCHAR}, #{createtime,jdbcType=TIMESTAMP}, #{detail,jdbcType=LONGVARCHAR}
      )
  </insert>

</mapper>